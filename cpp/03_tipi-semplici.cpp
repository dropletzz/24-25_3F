
int main() {
    // un carattere ASCII (8 bit)
    char lettera_a = 'a';
    char dollaro = '$';

    // un numero intero (32 bit)
    int num = 37;
    int num_negativo = -4;

    // un numero con la virgola (32 bit)
    float y = 2.34225;

    // un numero con la virgola piu' preciso (64 bit)
    double yy = 2.4654536;

    // un valore booleano (vero/falso)
    bool a = true;
    bool b = false;
}
